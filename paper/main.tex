%File: main.tex  (ICLR 2027 anonymous submission)
\documentclass{article} % For LaTeX2e
\usepackage{iclr2027_conference,times}

% Optional math commands from https://github.com/goodfeli/dlbook_notation.
% Disabled: the paper carries its own notation (see NOTATION.md); loading the
% dlbook macros risks silent clashes.
%\input{math_commands.tex}

\usepackage{hyperref}
\usepackage{url}
\usepackage{graphicx}

% Project packages
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{booktabs}
\usepackage{bm}
\usepackage{xspace}

% Macros
\newcommand{\ssppp}{SS2P2\xspace}
\newcommand{\sppp}{S2P2\xspace}          % the published model of Chang et al.
\newcommand{\spppu}{S2P2\xspace}         % our factorized, uncapped benchmark variant
                                          % (same label by author choice; its ablation
                                          % status is disclosed in Background)
\newcommand{\spppub}{S2P2\textsubscript{pub}\xspace} % faithful published configuration
\newcommand{\nhp}{\textsc{nhp}\xspace}

% Float placement: the experiments section carries six large floats; defaults
% strand them on trailing float pages.
\renewcommand{\topfraction}{0.95}
\renewcommand{\textfraction}{0.05}
\renewcommand{\floatpagefraction}{0.8}

\title{SS2P2: A Stable State-Space Point Process \\ for Limit-Order-Book Simulation}

% Authors must not appear in the submitted version. They should be hidden
% as long as the \iclrfinalcopy macro remains commented out below.
% Non-anonymous submissions will be rejected without review.
\author{Anonymous authors\\
Paper under double-blind review}

%\iclrfinalcopy % Uncomment for camera-ready version, but NOT for submission.
\begin{document}

\maketitle

\input{section/abstract}

\input{section/intro}
\input{section/background}
\input{section/method}
\input{section/experiments}
\input{section/conclusion}

\subsection*{AI use statement}
% Required by ICLR 2027; does not count toward the page limit.
% TODO(author): replace the bracketed placeholders per the ICLR 2027 AI Policy.
In this work, we used generative AI tools for [tasks with required disclosure].
We have not used generative AI tools for [other tasks with required
disclosure], and [the rest of the required disclosure tasks] are not
applicable to this work. We have reviewed all AI-assisted work and take
responsibility for the final content of this work, including text, claims,
and artifacts produced with the aid of generative AI.

\subsection*{Reproducibility statement}
% Recommended by ICLR 2027; does not count toward the page limit.
% TODO(author): add the code URL once the anonymized repository exists.
Appendix~\ref{sec:appendix} states the rate-bound propositions with proofs,
the full calibration protocol with the per-checkpoint multipliers $\kappa$,
the metric definitions (NLL quadrature, conditional type accuracy,
expected-time MAE), the order-flow proxy behind the stylized-fact battery,
the complete model and training configuration (optimizer, schedule, seeds,
$z_{\max}$, $\varsigma$ initialization), and the dataset facts (assets,
splits, event vocabulary, test-event counts). Every roll-out is seeded and
deterministic, and scripts that regenerate every table and figure from the
raw results accompany the submission.

\bibliography{orderbook_references,references}
\bibliographystyle{iclr2027_conference}

\appendix
\input{section/appendix}

\end{document}
