% AAAI-safe macro/package header for TFOW.
% NOTE: the following are FORBIDDEN by aaai2026 and were removed from the original
% (ACM) header: hyperref, float, stfloats, geometry. The aaai2026 class already
% loads `caption` (with its own settings) and sets the bibliography style, so we
% must NOT reload caption or set \bibliographystyle here.
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{acro}                 % acronyms: \ac{...}, \acp{...}
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{csquotes}
\usepackage{wasysym}
\usepackage{multirow}
\usepackage{pdflscape}
\usepackage{multicol}
\usepackage{arydshln}
\usepackage{adjustbox}
\usepackage{subcaption}           % loads caption if needed (already loaded by class)
\usepackage{array}
\usepackage{xspace}
\usepackage{longtable}
\usepackage{bm}
\usepackage{makecell}
\usepackage{threeparttable}
\usepackage{afterpage}
\usepackage{placeins}
\usepackage{algorithm}            % AAAI-recommended; provides the algorithm float
\usepackage{algorithmicx}
\usepackage{algpseudocode}
\providecommand{\mathbbm}[1]{\mathbb{#1}}  % bbm fonts not deployed; \mathbb fallback

% --- macros carried over from the original TFOW main preamble ---
% Figure that degrades to a placeholder box until the asset is fetched.
\newcommand{\autofig}[2]{\IfFileExists{#1}{\includegraphics[width=#2]{#1}}{\fbox{pending figure: \texttt{\detokenize{#1}}}}}
% Run-in italic subparagraph heading (kept identical across classes).
\providecommand{\subparagraph}[1]{\par\smallskip\noindent\textit{#1}\hspace{0.5em}}
% hyperref is forbidden by AAAI -> \autoref degrades to a plain reference.
\providecommand{\autoref}[1]{\ref{#1}}

\usepackage[inline]{enumitem}
\setlist[itemize]{leftmargin=*, itemindent=8pt}
\setlist[enumerate]{leftmargin=0pt,itemindent=16pt,label=\roman*)}

% ---- Acronyms --------------------------------------------------------------
\DeclareAcronym{lob}{short = LOB, long = limit order book}
\DeclareAcronym{cex}{short = CEX, long = centralized exchange}
\DeclareAcronym{dex}{short = DEX, long = decentralized exchanges}
\DeclareAcronym{tfow}{short = TFOW, long = Tempo Focused Order Watcher}
\DeclareAcronym{setmtpp}{short = set-mtpp, long = set-valued marked temporal point process}
\DeclareAcronym{tpp}{short = TPP, long = temporal point process}
