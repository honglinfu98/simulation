\section{Methods}
\label{sec:method}

To apply point process modeling to high-frequency LOB data, we represent
market dynamics as a sequence of \textbf{events} (pipeline in
Figure~\ref{fig:pipeline}): we collect the data, construct events, and train
\ssppp{} to learn the underlying dynamics.

\subsection{Data collector}
\label{subsec:data-collector}
We obtain trade and Level-2 limit order book data from Kaiko
\citep{Kaiko2025TheKaiko}, an institutional provider of digital-asset market
data. Each update record carries a timestamp, an update type (snapshot or
liquidity update), and price--volume information per bid and ask level.
Trades are retrieved via the same API and attributed to the book change
whose update interval contains them, their volume netted against the
liquidity change at the traded price; the event constructor classifies
every observed book change through this alignment.
% AUTHOR ACTION REQUIRED: report data-quality statistics (unmatched trade
% count, ambiguous matches, dropped updates, multi-event timestamps) in the
% appendix; collection dates and timezone must also be stated there.





\subsection{Event constructor}
\label{subsec:event-construction}
\begin{figure}[t]
\centering
\includegraphics[width=0.9\columnwidth]{figs/pipeline}
\caption{Pipeline. (1)~\emph{Data collector}: aligned trade records and
Level-2 book updates. (2)~\emph{Event constructor}: consecutive snapshots
are differenced and aligned with trades to emit atomic typed events.
(3)~\emph{Model training}: the event sequence drives the state-space
backbone whose hidden representation $h_t$ decodes the bounded total rate
$\lambda(t)$ and mark distribution $p(e\mid t)$, giving
$\lambda_e(t)=\lambda(t)\,p(e\mid t)$.}
\label{fig:pipeline}
\end{figure}
\input{exhibits/event_construction} 



We represent market dynamics as a sequence of \textbf{events} over the top
$K=10$ levels on each side.

\paragraph{Atomic Events.}
We define a finite set $\mathcal{E}$ of \emph{atomic event types}
$e=(c,s,\delta)$: a class $c\in\{\text{LO},\text{MO},\text{CO},\text{IS}\}$
--- limit order (liquidity provision at a top-$K$ level), market order
(consumption at the best quote), cancel order (withdrawal at a top-$K$
level), or inside-spread order (a new order improving the prevailing best
quote); a side $s\in\{b,a\}$; and a level index $\delta$. For LO/CO,
$\delta\in\{1,\dots,K\}$ is the distance from the best quote ($\delta=1$ =
best bid/ask); for MO, $\delta=1$ always; for IS, $\delta\in\{1,\dots,K\}$
is the number of ticks by which the new quote improves the spread.

\paragraph{Event Construction from LOB and Trade Data.}
As illustrated in Figure~\ref{fig:graph_infer}, atomic events are inferred
by differencing consecutive Level-2 snapshots and aligning them with trades
in the inter-update interval, via a three-step decision tree. A liquidity
decrease with a matching trade at that price is a \textbf{Market Order}
(recorded at $\delta=1$; any residual decrease becomes a cancellation); a
decrease with no matching trade is a \textbf{Cancel Order}; an increase is
an \textbf{Inside-Spread Order} if inside the previous spread (indexed by
tick distance from the previous best) and otherwise a \textbf{Limit Order}
(indexed by its post-update level). One update can emit several events; they
share its timestamp and are ordered deterministically (MO, CO, LO, IS; bid
before ask; then by level). Order sizes are retained in the dataset but the
models use only event times and types.
% AUTHOR ACTION REQUIRED: confirm how IS improvements deeper than 10 ticks
% are mapped into the 62-type vocabulary (constructor emits uncapped tick
% distances; idx_to_event.json holds IS levels 1..10 only).


\subsection{Stable State-Space Point Process (SS2P2)}
\ssppp{} factors the per-type intensity into a bounded total rate and a
mark distribution --- the ground-intensity--mark factorization of set-valued
MTPPs \citep{chang2024probabilistic} --- both decoded from the \emph{same}
continuous-time hidden representation $h_t$:
$\lambda_e(t)=\lambda(t)\,p(e\mid t)$, with
$\sum_{e}\lambda_e(t)=\lambda(t)$ since $\sum_{e} p(e\mid t)=1$. The design
principle is to put nonlinearity where it cannot perturb the rate.



\subsection{Softmin-bounded rate head}
An uncapped head can fail in closed loop through feedback: each sampled
event raises the intensity, faster events follow, and with nothing bounding
the rate the roll-out drifts hot. The rate head is built to break this
loop. From the backbone's hidden representation $h_t$ (LayerNorm'd, hence
bounded in scale), a gate produces a bounded state $g$, read out through a
smooth one-sided cap with fixed constant $z_{\max}$ ($z_{\max}{=}6$ in all
experiments):
\begin{align}
o &= \sigma(W_o h_t + b_o)\in(0,1)^H,\notag\\
g &= o\odot\tanh(h_t)\in(-1,1)^H,\notag\\
z_{\mathrm{raw}} &= w^{\!\top}g+b,\notag\\
z &= z_{\max}-\mathrm{softplus}\bigl(z_{\max}-z_{\mathrm{raw}}\bigr),\notag\\
\lambda(t) &= \varsigma\cdot\mathrm{softplus}(z)\notag
\end{align}
with a learnable positive scale $\varsigma$ initialized from the
training-split rate ($b=0$ at initialization, so $g\!\approx\!0$ gives
$\lambda\approx\varsigma\ln 2$ when $z_{\max}\!\gg\!0$; appendix).

\begin{itemize}
\item \textbf{Hard ceiling.} Since $\mathrm{softplus}(x)>0$ for every $x$,
  $z\le z_{\max}$ holds identically, so for every state and parameter
  setting $\lambda(t)\le\bar\lambda:=\varsigma\,\mathrm{softplus}(z_{\max})$.
  The intensity is globally bounded, the event count on any finite horizon
  $[0,T]$ is stochastically dominated by a homogeneous Poisson count with
  mean $\bar\lambda T$ (finite-time non-explosion), and $\bar\lambda$ is an
  \emph{exact} dominating rate for Ogata thinning (Proposition~1, appendix).
  The bound guarantees no more: stationarity, ergodicity, and realism are
  assessed empirically.
\item \textbf{Bounded input.} The readout sees $g\in(-1,1)^H$: however far
  the closed loop drives $h_t$, an extreme state changes the direction of
  $g$ but not its magnitude. For fixed parameters this already gives
  $|z_{\mathrm{raw}}-b|<\lVert w\rVert_1$, a computable parameter-dependent
  ceiling even without the cap; the cap adds a ceiling that is
  \emph{user-chosen, independent of $w$ and $b$, and invariant under
  training}.
\item \textbf{Vanishing gain.} Differentiating the cap gives
  $\partial z/\partial z_{\mathrm{raw}} = \sigma(z_{\max}-z_{\mathrm{raw}})$:
  as a burst pushes the intensity toward the ceiling, the head becomes
  exponentially \emph{less} sensitive to further state excursions, so the
  feedback loop loses gain exactly when it is most dangerous. In our
  experiments this coincided with a closed-loop rate that responds smoothly
  to rescaling (see Calibratability).
\item \textbf{One-sided cap.} The cap distorts only the high end: for
  $z_{\mathrm{raw}}$ well below $z_{\max}$, $z\approx z_{\mathrm{raw}}$ and
  the log-intensity derivative approaches one, so quiet-period gradients
  pass essentially unattenuated. Because $g$ is bounded, the intensity has a
  strictly positive, parameter-dependent floor (typically numerically
  negligible; Proposition~2, appendix) rather than an exact zero.
\end{itemize}
The first three choices are intended to control the intensity ceiling and
its sensitivity; the fourth keeps the bound from distorting prediction when
the market is quiet.

\subsection{Rate-neutral mark head}
Marks are decoded from the same $h_t$ and are \emph{instantaneously
rate-neutral at a fixed hidden state}:
\[
p(e\mid t)=\mathrm{softmax}\bigl(\mathrm{MLP}(h_t)\bigr)_e,
\qquad
\lambda_e(t)=\lambda(t)\,p(e\mid t),
\]
so $\sum_e\lambda_e(t)=\lambda(t)$ exactly, whatever the mark logits.
Because the soft-max lives on the simplex, the mark network can be
arbitrarily deep, or later conditioned on exogenous state such as an agent's
resting quotes, without changing the total rate at that state or the
architectural ceiling. Two qualifications: in closed loop, a changed mark
distribution changes sampled marks, hence future states and rates (the
ceiling holds, but the realized mean rate can shift and may require
recalibration); and both heads read the shared backbone, so mark gradients
still shape $h_t$ --- only the head-specific parameters separate. The
simplex guarantees rate neutrality state by state, not trajectory by
trajectory.

\subsection{Likelihood and sampling}
Training minimizes the negative log-likelihood of each training window,
covering its events $(t_i,e_i)$ and the next event after the window:
\[
\mathcal{L}
= \int_{0}^{T}\!\lambda(u\mid\mathcal{H}_u)\,du
\;-\;\sum_{i}\Bigl[\log\lambda(t_i) + \log p(e_i\mid t_i)\Bigr],
\]
with $T$ the next-event time. The integral and the $\log\lambda$ terms score
\emph{when} events happen (the compensator charges for intensity placed where
no event occurred); the $\log p$ term is a categorical cross-entropy scoring
\emph{what} each event is. By the factorization, the timing terms are the
only loss on the rate head's parameters and the mark term the only loss on
the mark head's; the shared backbone receives both gradients. Each
$\lambda(t_i)$ is evaluated at the left limit, so an event never informs its
own intensity. During \emph{training}, the compensator is approximated by
one intensity evaluation per inter-event interval (the endpoint rule), which
under-weights a decaying intensity and biases the learned rate upward; the
post-hoc calibration below compensates the resulting inflation of the
realized closed-loop mean rate, not the likelihood approximation itself.
Held-out NLL is evaluated with a denser 32-point log-spaced quadrature per
gap. Closed-loop sampling uses Ogata thinning with the exact dominating rate
$\bar\lambda=\varsigma\cdot\mathrm{softplus}(z_{\max})$: propose arrivals
from a homogeneous Poisson process at the ceiling and accept each with
probability $\lambda(t)/\bar\lambda$.

\subsection{Carried-state simulation}
The standard roll-out protocol re-encodes a sliding window of the last $W$
events from a zero state at every step: $O(W)$ work per simulated event and
a hard memory ceiling --- nothing older than $W$ events can influence the
future, so long-memory stylized facts are \emph{architecturally} unreachable
regardless of the model. Because the backbone is a state-space recursion,
both costs vanish: we carry the packed decoder state and advance it with
only the new event, mathematically equivalent to encoding the entire history
from the seed. The state update and each thinning proposal cost $O(1)$ in the
history length (expected proposals per accepted event =
$\bar\lambda/\lambda$), removing the $O(W)$ re-encode and the memory
ceiling. Attention- and window-shaped baselines have no such Markov state
and retain the sliding window.

\subsection{Stateful (TBPTT) training}
Windowed maximum likelihood encodes every training window from a cold
initial state, so the model only ever trains in a ``history just began''
transient and MLE explains the observed rate with an inflated baseline ---
the free roll-out, whose state is warm, then double-counts. We train instead
with truncated backpropagation through time \citep{williams1990tbptt}: batch
lanes walk contiguous stretches of the stream in order, and the decoder
state at the end of window $t$ is passed, \emph{detached}, as the initial
state of window $t{+}1$ (resets only at genuine stream boundaries). Gradients
remain window-local; state values flow from the start of the stream.
Training is thus consistent with carried-state simulation at unchanged
per-step cost; it requires non-overlapping windows (stride $=$ window
length), and our controls match that setting.

\subsection{Post-hoc rate calibration}
\label{sec:calibration}
The factorization buys one more property: $\lambda = \varsigma\cdot\mathrm{softplus}(z)$ is
\emph{exactly linear} in the learnable scale $\varsigma$, the thinning ceiling
$\varsigma\cdot\mathrm{softplus}(z_{\max})$ scales with it, and mark
probabilities are untouched by construction. The free-running mean rate is
nonlinear in $\varsigma$ through the closed-loop feedback; we do not prove
it monotone, but it was monotone over every successful bracket in our
experiments. We therefore calibrate by a one-dimensional geometric bisection
on a multiplier $\kappa$: probe roll-outs at $\kappa\,\varsigma$ bracket the
target, then bisect in log space (full protocol in the appendix). The
result, when it verifies, is a simulator whose thinning bound stays exact
(the ceiling scales with $\kappa\varsigma$) and whose realized mean rate
matched the target within tolerance at the rates we examined; the
calibration is empirical, not exact. For \emph{any} decoder, a constant
$\kappa$ multiplying the sim-time intensity preserves conditional type
ratios $\lambda_e/\lambda$ at a fixed history (marginal mark frequencies can
still shift in a new roll-out, since histories change), so the same
bisection applies to every baseline; what is lost off-family is only the
exactness of the thinning bound. The procedure is falsifiable (targets,
tolerances, and verification in Calibratability and the appendix);
unresolved failures exclude the checkpoint and are reported as results, and
transfer to the test split is reported, never gated. We use calibration as
a \emph{post-hoc} correction for the realized-rate inflation caused by the
biased endpoint-rule training compensator --- and, in the experiments, as a
measurement instrument in its own right.
