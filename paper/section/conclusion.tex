\section{Discussion and Conclusion}
\label{sec:conclusion}

We held six neural MTPPs to both sides of the prediction--simulation tension
on three Coinbase assets at production event rates ($24$--$48$ events/s),
under a protocol in which failures are visible: lossless streaming
prediction, multi-seed confidence intervals, and verified rate calibration
whose failures count as results. The \ssppp{} heads
win every prediction metric on SOL, lead ETH, tie the strongest baseline on
BTC marks, and remain verifiably calibratable with competitive simulated
structure throughout. The sharpest finding is that \emph{calibratability is a
model property}: the unbounded parent head fails verified rate calibration on
7 of 9 checkpoints, including all three on SOL, with a critical-point
signature, while the bounded head verifies on all nine, confirming the
stability certificate empirically. Because the mark head
is rate-neutral, conditioning it on exogenous state such as an agent's
resting quotes cannot destabilize the rate, so \ssppp{} can serve as an
environment model for market-making world models. That action-conditioned
extension is future work. The same factorized intensity supports the
importance-sampling queries of \citet{chang2024probabilistic}, which map onto
quantities a market maker needs: fill probability and adverse-selection risk.
All tables and figures are regenerated by the released scripts.
